# Core
httpx>=0.27
selectolax>=0.3.21
beautifulsoup4>=4.12
playwright>=1.44        # run: playwright install chromium
pandas>=2.2
openpyxl>=3.1
PyYAML>=6.0
flask>=3.0
lxml>=5.0

# Production WSGI servers (gunicorn for Linux/Render, waitress for Windows).
gunicorn>=21.2 ; platform_system != "Windows"
waitress>=3.0

# Postgres backend — only used when DATABASE_URL is set (else SQLite; see database/pg.py).
psycopg[binary]>=3.2

# Optional error tracking — only active if SENTRY_DSN is set (see app.py).
sentry-sdk>=2.0
